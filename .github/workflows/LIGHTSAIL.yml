name: Deploy to LIGHTSAIL

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"

      - name: Grant execute permission for Gradle
        run: chmod +x gradlew

      - name: Build jar (skip tests)
        run: ./gradlew bootJar -x test

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.LIGHTSAIL_HOST }}
          username: ${{ secrets.LIGHTSAIL_USER }}
          key: ${{ secrets.LIGHTSAIL_SSH_KEY }}
          port: "22"
          script_stop: true
          command_timeout: 30m
          envs: DEPLOY_PATH,CONTAINER_NAME,HOST_PORT,CONTAINER_PORT
          script: |
            set -e

            if [ -z "$DEPLOY_PATH" ]; then
              echo "DEPLOY_PATH is empty"
              exit 1
            fi

            cd "$DEPLOY_PATH"
            git pull origin main

            cat > src/main/resources/application.yml << 'EOF'
            ${{ secrets.APPLICATION_YML }}
            EOF

            cat > src/main/resources/application-secret.yml << 'EOF'
            ${{ secrets.APPLICATION_SECRET_YML }}
            EOF

            chmod +x gradlew
            ./gradlew bootJar -x test

            docker build -t "$CONTAINER_NAME:latest" .
            docker rm -f $(docker ps -aq) || true
            docker run -d \
              --name "$CONTAINER_NAME" \
              --restart unless-stopped \
              -p "$HOST_PORT:$CONTAINER_PORT" \
              "$CONTAINER_NAME:latest"
        env:
          DEPLOY_PATH: ${{ secrets.LIGHTSAIL_PROJECT_PATH }}
          CONTAINER_NAME: ${{ secrets.DOCKER_CONTAINER_NAME || 'galleryreservation-app' }}
          HOST_PORT: ${{ secrets.HOST_PORT || '80' }}
          CONTAINER_PORT: ${{ secrets.CONTAINER_PORT || '8080' }}
